class Solution {
          
    public boolean hasAlternatingBits(int n) {
        int curr=-1;
       while(n>0)
       {
         if(curr==n%2)
         {
            return false;
         }
         curr=n%2;
         n=n/2;
       } 
       return true;
    }
}
